# IDEs and editors
.idea/
.ipynb_checkpoints/

# Python environment and build
__pycache__/
.venv/
*.egg-info/

# Logs and training output (all per-run artifacts live under runs/<exp_id>/)
runs/
outputs/
*.log
*.profile

# Jupyter notebooks
*.ipynb

# Local game results (per-run ratings.txt now lives inside outputs/<date>/<time>/;
# the root-level ratings.txt is legacy from older runs and ignored)
ratings.txt
game_history.pgn

# Legacy dependency management
Pipfile

# JavaScript / Playwright
node_modules/
app/dist-types/
test-results/
tests/app_e2e/screenshots/
.DS_Store
